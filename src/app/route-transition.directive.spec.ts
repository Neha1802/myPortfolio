import { RouteTransitionDirective } from './route-transition.directive';

describe('RouteTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
